CREATE TABLE peliculas (
    id_pelicula INTEGER,
    titulo CHAR(100)
);

CREATE TABLE peliculas2 (
    id_pelicula INTEGER,
    titulo CHAR(100),
    fecha DATETIME,
    isTrue BOOLEAN,
    salary FLOAT
);